<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.ex.mappers.testMapper">

	<!-- 회원 리스트 불러오기 -->
	<select id="MemberList" resultType="MemberDTO">
		SELECT * FROM jfactory_member where mem_is_admin=0
	</select>
	
	<!-- 관리자 리스트 불러오기 -->
	<select id="AdminList" resultType="AdminDTO">
		SELECT * FROM jfactory_member where mem_is_admin=1 or mem_is_admin=2
	</select>
	
	<!-- qna 글 공지 우선 불러오고 최신순으로 불러오기 -->
	<select id="QnaList" resultType="MemberqnaDTO">
		SELECT * FROM jfactory_qna ORDER BY qna_pin DESC, qna_datetime DESC
	</select>
	
	<!-- 회원가입 -->
	<insert id="Join" parameterType="MemberDTO">
		INSERT INTO jfactory_member values(#{mem_id},#{mem_email},#{mem_password},#{mem_realname},DEFAULT,0,#{mem_phone},#{mem_phone1},#{mem_phone2},#{mem_birth_year},#{mem_birth_month},#{mem_birth_day},#{mem_zipcode},#{mem_address1},#{mem_address2},0,0,#{mem_email_cert},DEFAULT,0,null,#{mem_sex},#{mem_skintype},#{mem_skintrouble1},#{mem_skintrouble2},0,0,0)
	</insert>
	
	<!-- 로그인 -->
	<select id="Login" resultType="MemberDTO">
		SELECT * FROM jfactory_member where mem_id=#{mem_id} AND mem_password = #{mem_password}
	</select>
	
	<!-- qna 문의 남기기 -->
	<insert id="QnaWrite" parameterType="MemberqnaDTO">
		INSERT INTO jfactory_qna values(DEFAULT,#{mem_id},#{qna_title},#{qna_content},null,null,DEFAULT,null,null,#{qna_category},0)
	</insert>
	
	<!-- qna 상세 내용 보기 -->
	<select id="QnaDetail" resultType="MemberqnaDTO">
		SELECT qna_no, mem_id, qna_title, qna_content, qna_reply_content, qna_category FROM jfactory_qna where qna_no=#{qna_no}
	</select>
	
	<!-- id 중복체크  -->
	<select id="IdChk" resultType="int">
		SELECT COUNT(*) FROM jfactory_member WHERE mem_id=#{mem_id}
	</select>
	
	<!-- review insert -->
	<insert id="Reiew">
		INSERT INTO jfactory_review values(DEFAULT,#{mem_id},#{re_title},#{re_score},#{re_content},DEFAULT,DEFAULT,#{re_org_file},#{re_stored_file},#{pd_name})
	</insert>
	
	<select id="FindId" resultType="MemberDTO">
		SELECT * FROM jfactory_member WHERE mem_realname=#{mem_realname} AND mem_email=#{mem_email}
	</select>
	
	<select id="ReviewView" resultType="ReviewDTO">
		SELECT * FROM jfactory_review WHERE re_no=#{re_no}
	</select>
	
	<select id="ReviewList" resultType="ReviewDTO">
		SELECT * FROM jfactory_review ORDER BY re_datetime DESC
	</select>
	
		<!-- 비밀번호 찾기 -->
	<select id="findPw" resultType="MemberDTO">
		SELECT * FROM jfactory_member WHERE mem_realname=#{mem_realname} AND mem_email=#{mem_email} AND mem_id=#{mem_id}
	</select>
	
	<!--  비밀번호 입력 -->
	<update id="setPw" parameterType="MemberDTO">
		UPDATE jfactory_member SET mem_password=#{mem_password} WHERE mem_id=#{mem_id}
	</update>
	
	<insert id="AddProduct">
		INSERT INTO jfactory_product values(DEFAULT,#{pd_name},#{pd_simplecontent},#{pd_price},#{pd_main_file},#{pd_main_stored_file},null,null,DEFAULT,DEFAULT,DEFAULT,DEFAULT,DEFAULT,#{pd_category},#{pd_tag1},#{pd_tag2},#{pd_tag3},#{pd_tag4},#{pd_tag5})
	</insert>
	
	<select id="ProductView" resultType="ProductDTO">
		SELECT * FROM jfactory_product WHERE pd_name=#{pd_name}
	</select>
	
	<update id="AddDetail" parameterType="ProductDTO">
		UPDATE jfactory_product SET pd_con_file=#{pd_con_file}, pd_con_stored_file=#{pd_con_stored_file} WHERE pd_name=#{pd_name}
	</update>
	
	
	<select id="ProductListView" resultType="ProductDTO">
		SELECT * FROM jfactory_product WHERE pd_category=#{pd_category}
	</select>
	
	<select id="BestReview" resultType="ReviewDTO">
		SELECT * FROM jfactory_review ORDER BY re_count DESC LIMIT 4
	</select>
	
	<update id="ReplyQna" parameterType="MemberqnaDTO">
		UPDATE jfactory_qna SET qna_reply_content=#{qna_reply_content}, qna_replay_datetime=DEFAULT, qna_response=1 WHERE qna_no=#{qna_no}
	</update>
	
	<insert id="WriteNotice" parameterType="MemberqnaDTO">
		INSERT INTO jfactory_qna values(DEFAULT,#{mem_id},#{qna_title},#{qna_content},null,null,DEFAULT,null,null,null,1)
	</insert>
	
	<insert id="ProductPhotoWrite" parameterType="hashMap">
      INSERT INTO jfactory_product values(DEFAULT,#{pd_name},#{pd_simplecontent},#{pd_price},null,#{pd_main_stored_file},null,null,DEFAULT,DEFAULT,DEFAULT,DEFAULT,DEFAULT,#{pd_category},#{pd_tag1},#{pd_tag2},#{pd_tag3},#{pd_tag4},#{pd_tag5})
   </insert>
	
</mapper>

