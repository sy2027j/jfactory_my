<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.ex.mappers.cartMapper">
	<!-- 장바구니 개별 상품 삭제 -->
    <delete id="delete" parameterType="int">
        delete from jfactory_cart where cart_id=#{cart_id}
    </delete>
    
    <insert id="AddCart">
    	INSERT INTO jfactory_cart values(DEFAULT,#{mem_id},#{pd_no},#{pd_name},#{pd_amount},DEFAULT,#{pd_img},#{pd_price})
    </insert>
	
	<select id="CartList" resultType="CartDTO">
		SELECT * FROM jfactory_cart WHERE mem_id=#{mem_id} ORDER BY cart_datetime DESC
	</select>
	
	<insert id="orderInfo">
		insert into jfactory_order(or_id, mem_id, buy_realname, buy_zipcode, buy_address1, buy_address2, buy_phone, or_price, or_cancel_state, or_datetime, re_check) values(#{or_id}, #{mem_id}, #{buy_realname},#{buy_zipcode}, #{buy_address1}, #{buy_address2}, #{buy_phone},#{or_price},DEFAULT,DEFAULT,DEFAULT)
	</insert>
	
	<insert id="orderdetailInfo">
		insert into jfactory_order_detail(or_id, pd_name, pd_amount, pd_price, pd_img) select #{or_id}, pd_name, pd_amount, pd_price, pd_img from jfactory_cart where jfactory_cart.mem_id=#{mem_id}
	</insert>
	
	<delete id="cartAllDelete">
		DELETE FROM jfactory_cart WHERE mem_id=#{mem_id}
	</delete>
	
	<select id="orderList" resultType="OrderDTO">
		select * from jfactory_order where mem_id=#{mem_id} ORDER BY or_datetime DESC
	</select>
	
	<select id="OrderDetail" resultType="OrderDetailDTO">
		select * from jfactory_order_detail where or_id=#{or_id}
	</select>
	
	<select id="Order_Detail" resultType="OrderDetailDTO">
		select * from jfactory_order_detail where or_id=#{or_id} and re_check=0
	</select>
	
	<select id="OrdermemDetail" resultType="OrderDTO">
		select * from jfactory_order where or_id=#{or_id} and re_check=0
	</select>
	
	<update id="OrderCancel" parameterType="OrderDTO">
		UPDATE jfactory_order SET or_cancel_state=1 WHERE or_id=#{or_id}
	</update>
	
	<update id="ReviewOk" parameterType="OrderDetailDTO">
		update jfactory_order_detail set re_check=1 where or_id=#{or_id} and pd_name=#{pd_name}
	</update>
	
</mapper>